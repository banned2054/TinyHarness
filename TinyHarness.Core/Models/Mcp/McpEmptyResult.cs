namespace TinyHarness.Core.Models.Mcp;

/// <summary>
///     ping 的空结果对象（序列化为 <c>{}</c>）。
///     The empty ping result object (serializes to <c>{}</c>).
/// </summary>
public sealed record McpEmptyResult;
